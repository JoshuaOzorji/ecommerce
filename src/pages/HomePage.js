import React from "react";
import { Contact, Hero, FeaturedProducts } from "../components";

const HomePage = () => {
	return (
		<div>
			<Hero />
			<FeaturedProducts />
			<Contact />
		</div>
	);
};

export default HomePage;
